# Dependencies
/node_modules

# Astro build output
/dist
/.astro

# Misc
.DS_Store
.env
.env.local

# Personal Claude Code notes — not shared via version control
CLAUDE.local.md

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright MCP browser snapshots
.playwright-mcp/

# Per-agent git worktrees (ADR 2026-09-13-git-worktrees-for-agent-isolation) —
# machine-local checkouts, never shared via version control.
.claude/worktrees/
