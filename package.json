{
  "name": "ckaserer-dev",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "generate-pdf": "node scripts/generate-cv-pdf.mjs",
    "generate-og": "node scripts/generate-og-image.mjs",
    "build:full": "npm run build && npm run generate-og && npm run generate-pdf"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource/carlito": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/browser-chromium": "^1.63.0",
    "playwright": "^1.63.0",
    "typescript": "^5.9.3"
  }
}
